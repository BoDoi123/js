// Ưu tiên ()
let diemToan = 9.5;
let diemVan = 8.75;

let dtb = (diemToan + diemVan) / 2;

console.log(dtb);

// Right To Left (Thứ tự ưu tiên trên cùng 1 dòng)
let x = (y = 25);

// Left To Right
let c = 25 - 7 + 8 - 1;
